# Copy this file's contents into the Streamlit Cloud app's Secrets panel.
# Do NOT commit real values here.
GROQ_API_KEY = "gsk_..."
HF_REPO_ID   = "your-hf-username/water-contamination-kg"
# HF_TOKEN is only required if the dataset repo is private
# HF_TOKEN = "hf_..."
