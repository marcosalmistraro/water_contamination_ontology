# Runtime dependencies for the Streamlit app (Streamlit Cloud deployment).
# Ingestion-only deps (geopandas, pdfplumber, owlready2, pyshacl, mlflow, etc.)
# are intentionally excluded — they are only needed locally for `make ingest`.
rdflib>=7.0
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6.0
requests>=2.31
python-dotenv>=1.0
streamlit>=1.35
streamlit-folium>=0.20
folium>=0.17
langchain>=0.2
langchain-groq>=0.1
langchain-core>=0.2
pyoxigraph>=0.4
huggingface_hub>=0.23
